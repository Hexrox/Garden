# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Garden App Frontend - Environment Variables
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# API Backend URL
#
# For local development (when running backend on localhost:3001):
REACT_APP_API_URL=http://localhost:3001
#
# For production with nginx proxy:
# Leave EMPTY to use relative URLs (nginx will proxy /api to backend)
# REACT_APP_API_URL=
#
# For production with direct backend URL:
# REACT_APP_API_URL=https://api.gardenapp.pl
#
# ℹ️  RECOMMENDED FOR PRODUCTION:
#    Leave REACT_APP_API_URL empty and configure nginx to proxy /api/* to backend
#    This is more secure and flexible than hardcoding API URL in frontend
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
